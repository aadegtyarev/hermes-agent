name: google-docs
version: 1.0.0
description: "Google Docs integration — read a document's text by URL/id (READ-ONLY, documents.readonly scope). No write tools. Credentials mounted read-only, resolved parent-side."
author: multi-agent
kind: standalone
requires_env: []
provides_tools:
  - gdoc_read
provides_hooks: []
